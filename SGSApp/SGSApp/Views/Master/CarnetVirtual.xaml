<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:custom="clr-namespace:RoundedCornerViewDemo.ControlsToolkit.Custom;assembly=SGSApp" x:Class="SGSApp.Views.Master.CarnetVirtual" BackgroundColor="{StaticResource Gray}" BindingContext="{Binding Menu}">
    <ContentPage.Content>
        <StackLayout Padding="10">
            <custom:RoundedCornerView BorderColor="{StaticResource Gray}" BorderWidth="1" BackgroundColor="Transparent" VerticalOptions="FillAndExpand" RoundedCornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2.0*" />
                        <RowDefinition Height="8.0*" />
                    </Grid.RowDefinitions>
                    <StackLayout Grid.Row="0" Padding="0" BackgroundColor="{StaticResource Primary}">
                        <Label HeightRequest="30" Text="Identificación Virtual" HorizontalOptions="Center" FontSize="24" TextColor="{StaticResource Gray}" />
                        <Label x:Name="nombreUsuario" VerticalOptions="End" Text="John Lopez" HorizontalOptions="Center" TextColor="{StaticResource Light}" FontSize="36" />
                    </StackLayout>
                    <StackLayout x:Name="qrcode" Grid.Row="1" BackgroundColor="White" Padding="0,0">
                    </StackLayout>
                </Grid>
            </custom:RoundedCornerView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>