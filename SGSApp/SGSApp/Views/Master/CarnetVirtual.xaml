<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:RoundedCornerViewDemo.ControlsToolkit.Custom;assembly=SGSApp"
             x:Class="SGSApp.Views.Master.CarnetVirtual"
             BackgroundColor="{StaticResource Gray}"
             BindingContext="{Binding Menu}">
    <ContentPage.Content>
            <StackLayout Padding="10">
            <custom:RoundedCornerView BorderColor="{StaticResource Gray}" BorderWidth="3" BackgroundColor="Transparent" VerticalOptions="FillAndExpand" RoundedCornerRadius="8">
                <StackLayout x:Name="qrcode" BackgroundColor="#fafafa"></StackLayout>
                <Label HeightRequest="30"  Text="Identificación Virtual" HorizontalOptions="CenterAndExpand" FontSize="28" TextColor="{StaticResource GrayFont}"/>
            </custom:RoundedCornerView>
            </StackLayout>

    </ContentPage.Content>
</ContentPage>

